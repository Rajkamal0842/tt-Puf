# BEFORE (missing environment - causes error):
jobs:
  viewer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/deploy-pages@v4

# AFTER (correct - add these 3 lines):
jobs:
  viewer:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
